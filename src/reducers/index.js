import {
    ADD_CHILD_NODE
} from '../symbols';
const node = (state, action) => {
    switch (action.type) {
        case ADD_CHILD_NODE:
        default:
            return state;
    }
};

export default node;